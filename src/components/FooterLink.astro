---
import type { HTMLAttributes } from "astro/types"
interface Props extends HTMLAttributes<"a"> {}
import { cn } from "@/lib/utils"
---

<a
  class={cn(
    "text-muted-foreground hover:text-accent-foreground inline-flex items-center transition",
    Astro.props.class
  )}
  {...Astro.props}><slot /></a
>
